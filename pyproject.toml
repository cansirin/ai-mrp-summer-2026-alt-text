[project]
name = "alt-text-vlm"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "torch",
  "transformers",
  "accelerate",
  "pillow",
  "einops",
  "timm",
  "matplotlib",
  "scikit-learn",
]
